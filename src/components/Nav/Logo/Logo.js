import {Container, Text} from './Logo.styles'

const Logo = ({theme}) => {
    return ( 
        <Container theme={theme}>
            <Text>BDR</Text>
        </Container>
     );
}
 
export default Logo;